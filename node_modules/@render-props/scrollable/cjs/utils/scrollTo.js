'use strict'

exports.__esModule = true
exports.default = scrollTo

var _utils = require('@render-props/utils')

function scrollTo(element, from, to, {duration, timing}) {
  const scroller =
    element === window
      ? window.scrollTo
      : function(x, y) {
          element.scrollTop = y
          element.scrollLeft = x
        }
  ;(0, _utils.interpolate)(
    function(values) {
      scroller(values.x, values.y)
    },
    {
      from,
      to,
      duration,
      timing,
    }
  )
}
