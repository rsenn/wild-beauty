'use strict'

exports.__esModule = true
exports.default = exports.requestInterval = exports.clearRequestInterval = void 0

var _raf = _interopRequireWildcard(require('raf'))

var _performanceNow = _interopRequireDefault(require('performance-now'))

function _interopRequireDefault(obj) {
  return obj && obj.__esModule ? obj : {default: obj}
}

function _getRequireWildcardCache() {
  if (typeof WeakMap !== 'function') return null
  var cache = new WeakMap()
  _getRequireWildcardCache = function() {
    return cache
  }
  return cache
}

function _interopRequireWildcard(obj) {
  if (obj && obj.__esModule) {
    return obj
  }
  if (obj === null || (typeof obj !== 'object' && typeof obj !== 'function')) {
    return {default: obj}
  }
  var cache = _getRequireWildcardCache()
  if (cache && cache.has(obj)) {
    return cache.get(obj)
  }
  var newObj = {}
  var hasPropertyDescriptor =
    Object.defineProperty && Object.getOwnPropertyDescriptor
  for (var key in obj) {
    if (Object.prototype.hasOwnProperty.call(obj, key)) {
      var desc = hasPropertyDescriptor
        ? Object.getOwnPropertyDescriptor(obj, key)
        : null
      if (desc && (desc.get || desc.set)) {
        Object.defineProperty(newObj, key, desc)
      } else {
        newObj[key] = obj[key]
      }
    }
  }
  newObj.default = obj
  if (cache) {
    cache.set(obj, newObj)
  }
  return newObj
}

/**  Copyright 2011, Joe Lambert.
 **  Free to use under the MIT license.
 **  http://www.opensource.org/licenses/mit-license.php
 **/
const clearRequestInterval = handle => {
  ;(0, _raf.cancel)(handle.value)
}

exports.clearRequestInterval = clearRequestInterval

const requestInterval = (fn, delay) => {
  let start = (0, _performanceNow.default)()
  const handle = {}

  const loop = () => {
    if ((0, _performanceNow.default)() - start >= delay) {
      fn.call(null)
      start = (0, _performanceNow.default)()
    }

    handle.value = (0, _raf.default)(loop)
  }

  handle.value = (0, _raf.default)(loop)
  return handle
}

exports.requestInterval = requestInterval
var _default = requestInterval
exports.default = _default
